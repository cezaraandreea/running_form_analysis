# Copiaza acest fisier in .streamlit/secrets.toml si completeaza valorile.
# secrets.toml NU se comite in git (adauga-l in .gitignore).

[email]
smtp_host     = "smtp.gmail.com"   # sau smtp.office365.com, smtp.yahoo.com etc.
smtp_port     = 587                 # 587 = STARTTLS (recomandat), 465 = SSL
smtp_user     = "runanalyzer26@gmail.com"
smtp_password = "ubzr udjl xucv qjwp"  # Gmail: Settings > Security > App Passwords
sender_name   = "RunAnalyzer"
